{
  "schema": "agent-context-transport-profile/v2",
  "adapter": "pxpipe-context-plugin",
  "activation": "explicit-opt-in",
  "model": "{{model}}",
  "plugin_name": "{{plugin_name}}",
  "plugin_version": "{{plugin_version}}",
  "plugin_manifest_sha256": "{{plugin_manifest_sha256}}",
  "plugin_integrity_sha256": "{{plugin_integrity_sha256}}",
  "mcp_server_sha256": "{{mcp_server_sha256}}",
  "mcp_worker_sha256": "{{mcp_worker_sha256}}",
  "runtime_bundle_sha256": "{{runtime_bundle_sha256}}",
  "workflow_manifest_sha256": "{{workflow_manifest_sha256}}",
  "workflow_source_tree_sha256": "{{workflow_source_tree_sha256}}",
  "workflow_plugin_files_sha256": "{{workflow_plugin_files_sha256}}",
  "trusted_root_sha256": "{{trusted_root_sha256}}",
  "provenance_assurance": "content-and-install-anchored;no-host-signature",
  "source_sha256": "{{source_sha256}}",
  "analyze_receipt_path": "{{analyze_receipt_path}}",
  "analyze_receipt_sha256": "{{analyze_receipt_sha256}}",
  "selection": "analyze-then-render",
  "content_scope": "new-cold-reference-only",
  "session_boundary": "plugin-load-requires-new-chat",
  "wire_exactness": "lossy-selected-cold-reference;canonical-state-remains-native",
  "canonical_truth": [
    "task-and-context-capsules",
    "requirements-and-decisions",
    "current-and-open-protocol-state",
    "paths-ids-hashes-amounts-and-receipts"
  ],
  "requirement_contract_sha256": "{{requirement_contract_sha256}}",
  "task_invariant_sha256": "{{task_invariant_sha256}}",
  "measurement_gate": "analyze-receipt-plus-task-acceptance",
  "fallback": "native",
  "approval_source": "{{approval_source}}",
  "approval_receipt_sha256": "{{approval_receipt_sha256}}"
}
