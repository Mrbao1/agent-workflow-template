{
  "environment": "{{local_test_production}}",
  "branch_patterns": ["{{patterns}}"],
  "install": "{{install_command}}",
  "checks": ["{{check_commands}}"],
  "build": "{{build_command}}",
  "artifact": "{{artifact_path_or_digest}}",
  "deploy": "{{deploy_command_or_none}}",
  "health": "{{health_check}}",
  "cleanup": "{{cleanup_command}}",
  "rollback": "{{rollback_command}}",
  "human_approval": {{true_or_false}}
}
