interface:
  display_name: "pxpipe Proxy (Quarantined)"
  short_description: "Unavailable until verified provenance is restored"
  default_prompt: "Explain that pxpipe is quarantined; do not install, execute, or bypass its integrity gate."
policy:
  allow_implicit_invocation: false
