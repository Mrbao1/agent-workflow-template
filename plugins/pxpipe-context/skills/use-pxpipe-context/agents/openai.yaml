interface:
  display_name: "pxpipe Context (Quarantined)"
  short_description: "Unavailable until verified provenance is restored"
  default_prompt: "Explain that pxpipe is quarantined; do not invoke its MCP or bypass its integrity gate."
policy:
  allow_implicit_invocation: false
