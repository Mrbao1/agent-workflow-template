---
name: use-pxpipe-context
description: Quarantined compatibility Skill. Do not invoke the pxpipe MCP until verified v4 source and build provenance is present and publication is restored by review.
---

# pxpipe File Context — Quarantined

Stop. The optional MCP is unavailable while the plugin provenance status is
`quarantined`. Do not bypass the runtime integrity error or use this historical bundle
for instructions, requirements, state, diffs, tests, security, deployment, audit
evidence, secrets, or exact identifiers.

If a verified release is produced, model IDs must come from the configured exact
`PXPIPE_MCP_MODELS` allowlist and the original cold-reference/lossy-transport approval
boundary still applies.
